import {
  Canvas,
  Meta,
  Story,
  Preview,
  SourceState,
} from "@storybook/addon-docs";
import Text from "./Text";
import { Tab, TabContainer } from "./Tab";
import { CodeSnippets } from "@storybook/design-system";
import * as Form from "./Form.stories";
import FormGenerator from "../Components/FormGenerator";
import code from "!!raw-loader!../Components/FormGenerator/index.tsx";
import "./assets/styles.css";

<Meta title="Documentation/Overview" />

[![Stable Release](https://img.shields.io/npm/v/ccvfj-react-app.svg)](https://npm.im/ccvfj-react-app)
[![Blazing Fast](https://badgen.now.sh/badge/speed/blazing%20%F0%9F%94%A5/green)](https://npm.im/ccvfj-react-app)

# Welcome to <Text text={process.env.STORYBOOK_NAME} />

** <Text text={process.env.STORYBOOK_DESC} /> **

<p class="dir-rtl">
  با <Text text={process.env.STORYBOOK_NAME} /> میتوانید فرم های معمولی و
  پیشرفته خود را به صورت اختصاصی در پروژه های ریکتی خودتون بسازید.
</p>

# انگیزه

<p class="dir-rtl">
  همیشه ساخت فرم های پیشرفته در توسعه محصول کاری زمان بر و پر تکرار میباشد، به
  همین دلیل تصمیم بر این گرفتم ابزاری را توسعه دهم که بتوانم با آن تنها کامپونت
  های فرم خود را با هر ابزار و لایبری آماده کنم و پس از پیکربندی کامپونت با{" "}
  <Text text={process.env.STORYBOOK_NAME} /> در توسعه محصول از آن استفاده کنم.
</p>

# چرا <Text text={process.env.STORYBOOK_NAME+' ?'} />

<p class="dir-rtl">
  این ابزار میتواند با JSON فرم ها را بسازد و با کتاب خانه محبوب{" "}
  <a href="https://formik.org/">Formik</a> ساخته شده است.
</p>

### برخی از امکانات

- Form-level Validation
- Only Bootstrap Grid System
- Support all ui tools
- Nested form elements
- Communication between form elements

# Installation

You can install Formik with NPM, Yarn, or a good ol' `<script>` via [unpkg.com](https://unpkg.com/).

## NPM

```javascript
npm i ccvfj-react-app
```

Or

```javascript
yarn add ccvfj-react-app
```

# The Gist

<TabContainer>
  <Tab title="React" badge="TS">
    <Preview withToolbar className="my-0" isExpanded mdxSource={code}>
      <Form.Basic />
    </Preview>
  </Tab>
</TabContainer>
<CodeSnippets
  snippets={[
    {
      id: "1",
      renderTabLabel: () => "label",
      Snippet: () => Form.Basic,
    },
    {
      id: "2",
      renderTabLabel: () => "label 2",
      Snippet: Form.Basic,
    },
  ]}
/>
